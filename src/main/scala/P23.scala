package s99

import _root_.scala.util.Random

object P23 {
  def answer(): Unit = {
    val num = 3
    val list = List('a, 'b, 'c, 'd, 'f, 'g, 'h)
//    val r = new Random
//    def hoge(i: Int, ls: List[Symbol]): List[Symbol] = (i, ls) match {
//      case (_, Nil) => Nil
//      case (1, _) => hoge()
//      case (i, _) => ls(Random.netxInt(ls.length)) :: hoge(i - 1, ls)
//    }
    println(Random.nextInt())

  }
}
